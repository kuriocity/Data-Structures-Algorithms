package dsa.arrays;

import java.util.HashMap;
import java.util.HashSet;

import static com.utility.Utility.*;

public class Hashing {
    public static void main(String[] args) {
        HashSet<Integer> hashSet = new HashSet<>();
        HashMap<Integer, String> hashMap = new HashMap<>();

    }

    //pairs are not with self...
    static boolean pairWithGivenSum(int[] arr, int sum) {
        return false;
    }

    //prefix could be 0
    static boolean is0SumSubArray(int[] arr) {

        return false;
    }

    static int subArrayWithSum(int[] arr, int sum) {
        return -1;
    }

    //don't replace old hashmap value
    static int longestSubArrayWithGivenSum(int[] arr, int sum) {
        return -1;
    }

    static int longestCommonSpanSameSumBinaryArray(int[] arr) {
        return -1;
    }

    static int longestConsecutiveSubsequence(int[] arr) {
        return -1;
    }

    static int countDistinctElementsInWindow(int[] arr) {
        return -1;
    }

    static int moreThanNByKoccurences() {
        return -1;
    }

    //O(n*k) solution
    static int moreThanNByKoccurences2() {
        return -1;
    }
}
